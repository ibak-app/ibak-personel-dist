# İbak Personel — Dağıtım (OTA)
Bu repo İbak Personel app'inin OTA dağıtım kaynağıdır (kaynak kod değil).
- `latest.json` — en güncel sürüm işaretçisi (app bunu okur)
- Release asset'leri — APK dosyaları
